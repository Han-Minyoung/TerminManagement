<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
  
<mapper namespace="com.hmy.mapper.TerminDocumentMapper">

<!-- 	기일목록보기(기일 잡힌) -->
  <select id="getList1" resultType="com.hmy.domain.TerminDocumentVO">
    SELECT * FROM termindocument where td_termin is not null order by td_termin, td_time
  </select>
  <!-- 	기일목록보기(기일 아직 안잡힌) -->
  <select id="getList2" resultType="com.hmy.domain.TerminDocumentVO">
    SELECT * FROM termindocument where td_termin is null order by td_client
  </select>
  
<!--   기일등록 -->
  <insert id="insert">
  INSERT INTO termindocument (td_client, td_litigation_type, td_other_party, td_case_number, td_case_name,
  td_court, td_termin, td_time, td_court_room, td_termin_status, td_note)
   VALUES (#{td_client}, #{td_litigation_type}, #{td_other_party}, #{td_case_number}, #{td_case_name}, 
   #{td_court}, #{td_termin}, #{td_time}, #{td_court_room}, #{td_termin_status}, #{td_note} );
  </insert>
  
<!--   기일삭제 -->
  <delete id="delete">
    DELETE FROM termindocument where td_num=#{td_num}
  </delete>
  
<!--   기일부 수정 -->
  <update id="update">
  	UPDATE termindocument SET td_client=#{td_client}, td_litigation_type=#{td_litigation_type}, td_other_party=#{td_other_party},
  	td_case_number=#{td_case_number}, td_case_name=#{td_case_name}, td_court=#{td_court}, td_termin=#{td_termin},
  	td_time=#{td_time}, td_court_room=#{td_court_room}, td_termin_status=#{td_termin_status}, td_note=#{td_note} 
  	WHERE td_num=#{td_num}
  </update>
  
<!--   기일부 상세보기 -->
  <select id="readDetail" resultType="com.hmy.domain.TerminDocumentVO">
  	select * from termindocument where td_num=#{td_num}
  </select>
  
</mapper>